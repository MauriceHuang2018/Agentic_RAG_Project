# pnpm 11+ reads these instead of the legacy `pnpm` field in package.json.
# Approve esbuild + vue-demi build scripts (vitest's transpiler + EP dep).
only-built-dependencies[]=esbuild
only-built-dependencies[]=vue-demi