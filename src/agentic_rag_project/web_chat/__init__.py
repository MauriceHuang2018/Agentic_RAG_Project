"""Web Chat — chat UI backend (Vue3 frontend lands in M6, T6.1).

This package hosts the backend endpoints backing the chat UI: streaming
chat (NDJSON), conversation listing, message retrieval. Implemented in T6.1.
"""